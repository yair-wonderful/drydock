{
	"name": "@drydock/prototype",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"description": "The prototype domain: what a source tree is, what may be stored in one, and the limits it lives inside. Pure TypeScript with no dependencies — the browser and the server both hold it, so neither can disagree about what is valid.",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "node --test src/**/*.test.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^22.13.10",
		"typescript": "^5.9.3"
	}
}
